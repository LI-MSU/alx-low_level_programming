#include "main.h"

/**
 * _puts - is in charge of printing a string
 * @str: is a string
 * @a: is a var replacing \n
 */

void _puts(char *str)
{
	int a = 0;
	while (*(str  + a) != '\0')
	{
		putchar(*(str + a);
				a++
				}
putchar (10);
}
